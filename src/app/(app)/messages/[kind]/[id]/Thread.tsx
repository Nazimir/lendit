// Legacy file — superseded by /messages/[userId]/Thread.tsx.
// Kept as an empty stub so the (kind)/(id) route can still build until
// its parent page.tsx (now a redirect) is removed from the repo.
export {};
